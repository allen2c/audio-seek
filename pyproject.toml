[project]
authors = [{ name = "Allen Chou", email = "f1470891079@gmail.com" }]
dependencies = ["numpy", "soundfile"]
description = "Lightweight audio seeking across common formats: O(1) for uncompressed PCM (WAV/AIFF), best-effort frame-level seek for compressed formats (MP3/FLAC/OGG)."
license = { text = "MIT" }
name = "audio-seek"
readme = "README.md"
requires-python = ">=3.11,<4"
version = "0.2.0"

[project.urls]
Homepage = "https://github.com/allen2c/audio-seek"
"PyPI" = "https://pypi.org/project/audio-seek/"
Repository = "https://github.com/allen2c/audio-seek"

[tool.poetry]
packages = [{ include = "audio_seek" }]

[dependency-groups]
dev = [
  "black[jupyter]",
  "isort",
  "mkdocs-material",
  "poetry-plugin-export",
  "pytest",
  "pytest-asyncio",
  "pytest-cov",
  "pytest-env",
  "pytest-xdist",
  "rich",
  "rich-color-support",
  "ruff",
  "setuptools",
  "twine",
]

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
ignore = ["E203"]

[tool.pytest.ini_options]
env = ["ENVIRONMENT=test", "PYTEST_IS_RUNNING=true"]

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core>=2.0.0,<3.0.0"]
